{
  "report-type": "full",
  "moderate": false,
  "high": true,
  "critical": true,
  "allowlist": [
    // --- node-tar (transitive via npm bundled in node) ---
    "GHSA-34x7-hfp2-rc4v",
    "GHSA-83g3-92jg-28cx",
    "GHSA-8qq5-rm4j-mr97",
    "GHSA-9ppj-qmqm-q256",
    "GHSA-r6q2-hw4h-h46w",
    "GHSA-qffp-2rhf-9h96",
    // --- minimatch (transitive via eslint, npm, serve-handler) ---
    "GHSA-23c5-xmqv-rm74",
    "GHSA-3ppc-4f35-3m26",
    "GHSA-7r86-cg39-jmmj",
    // --- glob CLI injection (transitive via npm bundled in node) ---
    "GHSA-5j98-mcp5-4vw2",
    // --- rollup path traversal (devDep, not in production bundle) ---
    "GHSA-mw96-cpmx-2vgc",
    // --- @angular/core XSS i18n (peer devDep for angular adapter tests, not bundled) ---
    "GHSA-prjf-86w9-mfqv",
    "GHSA-g93w-mfhg-p222",
    // --- picomatch ReDoS (transitive via npm bundled, devDep only, not in production bundle) ---
    "GHSA-c2c7-rcm5-vvqj",
    // --- handlebars (transitive via semantic-release → conventional-changelog-writer, no fix available) ---
    "GHSA-2w6w-674q-4c4q",
    "GHSA-3mfm-83xf-c92r",
    "GHSA-9cx6-37pm-9jff",
    "GHSA-xhpv-hc6g-r9c6",
    "GHSA-xjpj-3mr7-gcpf",
    // --- lodash / lodash-es (transitive via @semantic-release/changelog, @semantic-release/git, semantic-release;
    //     devDep only, not in production bundle; no fix available in 4.x; _.template exploit path not reachable
    //     in semantic-release toolchain) ---
    "GHSA-r5fr-rjxr-66jc"
  ]
}
